<!DOCTYPE html>
<html>
<head>
  <title>Hackstars</title>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	
	<div class="background">
		<div class="container">
			<header>
				<a href="/"><%= image_tag "logo.png", id: "logo" %></a>
			</header>
			<nav class="well">
				<h4>View the TechStars Facebook</h4>
					<%= link_to "TechStars Facebook" + ' >', "http://hackstars.herokuapp.com/facestars/", { class: "btn secondary" } %>
				<h4>Meet the Hackstars:</h4>
				<ul>
					<% hackstars.each do |hackstar| %>
					<li> <%= link_to hackstar.name + ' >', hackstar, { class: "btn primary" } %> </li>
					<% end %>
				</ul>
				<h4>Meet the Associates:</h4>
				<ul>
					<% associates.each do |associate| %>
					<li> <%= link_to associate.name + ' >', associate, { class: "btn primary" } %> </li>
					<% end %>
				</ul>
			</nav>
	

			<div id="content" class="well">
			    <%= yield %>
			    <div id="profile">
			      <h1 id="name"></h1>
			      <img />
			      <p>
			    </div>
			</div>
		</div>
	</div>
	<%= render 'layouts/footer'%>
	

</body>
</html>
